
<div class="row flex-lg-row-reverse align-items-center g-5 py-4 mb-4">
    <div class="col-12 col-lg-6">
      <img src="/img/hero-image.png" width="607" height="510" class="d-block mx-lg-auto img-fluid" loading="lazy" alt="Cooking With Node.js">
    </div>
  
    <div class="col-12 col-lg-6">
      <h1 class="display-5 fw-bold mb-3">Ser & Estar</h1>
      <nav class="display-10 fw-bold mb-3">Pensamentos | Cultura | Aprendizagem</nav>
      <p class="lead">
        A marca de uma inteligência de primeira ordem é a capacidade de ter duas ideias opostas presentes no espírito ao mesmo tempo e nem por isso deixar de funcionar.

~F. Scott Fitzgerald
      </p>
  
      <div class="d-grid gap-2 d-md-flex justify-content-md-start">
        <a href="/explore-latest" class="btn btn-primary btn-dark btn-lg px-4 me-md-2">Últimas Postagens</a>
        <a href="/explore-random" class="btn btn-outline-secondary btn-lg px-4 me-md-2">Sortear Postagem</a>
      </div>
  
    </div>

    <!-- Categories Start -->

    <div class="row row-cols-2 row-cols-lg-6 g-2 g-lg-3 py-4">

      <% if(categories != '') { %>
        <% categories.forEach(function(category, index) { %>
          <a href="/categories/<%- category.name %>" class="col text-center category__link">
            <div class="category__img shadow">
              <img src="img/<%- category.image %>" alt="<%- category.name %>" loading="lazy">
            </div>
            <div class="pt-1"><%- category.name %></div>
          </a>
        <% }) %>
      <% } %>

    
        <a href="/categories" class="col text-center category__link">
        <div class="category__img shadow">
            <img src="img/view-all.jpg" alt="View All Categories" loading="lazy">
        </div>
        <div class="pt-1">Ver Todas</div>
        </a>
    
  
    </div>

    <!-- Categories End -->  

    <!-- Latest Start -->

<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
      <h2>Últimas Postagens</h2>
      <a href="/explore-latest" class="ms-auto">Ver Mais</a>
    </div>
  
    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        
      <% if(food != '') { %>
        <% food.latest.forEach(function(post, index){ %>
        <a href="/post/<%= post._id %>" class="col text-center category__link">
          <div class="category__img category__img--large shadow">
            <img src="uploads/<%= post.image %>" alt="<%= post.title %>" loading="lazy">
          </div>
          <div class="pt-1"><%= post.title %></div>
        </a>
        <% }) %>
      <% } else { %>
        <p>Postagem não encontrada.</p>
      <% } %>

        <p>Nem uma postagem encontrada</p>

    </div>
  
  </section>
  <!-- Latest End -->

<!-- Thai Start -->
<section class="pb-4 pt-4">
  <div class="d-flex mb-2 align-items-center">
    <h2>Thai posts</h2>
    <a href="/categories/Thai" class="ms-auto">Ver Mais</a>
  </div>

  <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
    <% if(typeof food.thai !== 'undefined' && food.thai.length > 0) { %>
      <% food.thai.forEach(function(post, index){ %>
      <a href="/post/<%= post._id %>" class="col text-center category__link">
        <div class="category__img category__img--large shadow">
          <img src="/uploads/<%= post.image %>" alt="<%= post.title %>" loading="lazy">
        </div>
        <div class="pt-1"><%= post.title %></div>
      </a>
      <% }) %>
    <% } else { %>
      <p>Nem uma postagem.</p>
    <% } %>
  </div>
</section>
<!-- Thai End -->

<!-- American Start -->
<section class="pb-4 pt-4">
  <div class="d-flex mb-2 align-items-center">
    <h2>American posts</h2>
    <a href="/categories/American" class="ms-auto">Ver Mais</a>
  </div>

  <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
    <% if(food.american != '') { %>
      <% food.american.forEach(function(post, index){ %>
      <a href="/post/<%= post._id %>" class="col text-center category__link">
        <div class="category__img category__img--large shadow">
          <img src="/uploads/<%= post.image %>" alt="<%= post.title %>" loading="lazy">
        </div>
        <div class="pt-1"><%= post.title %></div>
      </a>
      <% }) %>
    <% } else { %>
      <p>Nem uma postagem.</p>
    <% } %>
  </div>
</section>
<!-- American End -->

<!-- Chinese posts -->
<section class="pb-4 pt-4">
  <div class="d-flex mb-2 align-items-center">
    <h2>Chinese posts</h2>
    <a href="/categories/American" class="ms-auto">Ver Mais</a>
  </div>

  <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
    <% if(food.chinese != '') { %>
      <% food.chinese.forEach(function(post, index){ %>
      <a href="/post/<%= post._id %>" class="col text-center category__link">
        <div class="category__img category__img--large shadow">
          <img src="/uploads/<%= post.image %>" alt="<%= post.title %>" loading="lazy">
        </div>
        <div class="pt-1"><%= post.title %></div>
      </a>
      <% }) %>
    <% } else { %>
      <p>Nem uma postagem</p>
    <% } %>
  </div>
</section>
<!-- Chinese End -->
</div>
